# 🛠️ การ Build ผ่าน CI/CD (GitHub Actions) คืออะไร?

ถ้ามีคนถามว่า "ใช้วิธี Build แบบไหน?" หรือ "ทำไมต้องทำแบบนี้?" ให้ตอบแบบนี้ครับ:

## 🗣️ คำตอบแบบสั้น (Elevator Pitch)
> **"เราใช้ระบบ CI/CD ผ่าน GitHub Actions ครับ คือพอเรา Push โค้ดขึ้นไป GitHub ปุ๊บ มันจะเอาระบบ Cloud มาช่วย Build และตรวจสอบความถูกต้องให้เราอัตโนมัติบน Environment ที่สะอาด (Linux) ทำให้มั่นใจว่าโค้ดไม่มี Error และพร้อมขึ้น Production ครับ"**

---

## 📚 ขยายความ (ถ้าเขาถามต่อ)

วิธีที่เราทำเรียกว่า **"Cloud-based Build Pipeline"** ครับ โดยมีขั้นตอนการทำงานคือ:

1.  **Code Push:** พอเราแก้โค้ดเสร็จแล้วกด Push ขึ้น GitHub
2.  **Trigger:** GitHub จะรู้ทันทีว่ามีของใหม่มา แล้วเรียก "Runner" (เครื่องคอมพิวเตอร์จำลองบน Cloud) ขึ้นมาทำงาน
3.  **Environment Setup:** Runner ตัวนี้จะเป็น Linux ที่คลีนมากๆ (ไม่มีขยะ หรือปัญหา Path ยาวๆ แบบ Windows) มันจะลง Node.js และโปรแกรมที่จำเป็นใหม่หมด
4.  **Auto Build:** จากนั้นมันจะรันคำสั่ง `npm install` และ `npm run build` ให้เรา
5.  **Result:**
    *   ✅ **ถ้าผ่าน:** เราจะได้ไฟล์ Build (Artifact) ที่พร้อมเอาไป deploy บน Server จริงได้เลย
    *   ❌ **ถ้าไม่ผ่าน:** มันจะแจ้งเตือนทันที เราก็แก้แล้ว Push ใหม่

### 💡 ทำไมวิธีนี้ถึงดีกว่า Build ในเครื่องตัวเอง?
1.  **แก้ปัญหา "เครื่องฉันรันได้นะ":** เคยไหมที่เครื่องเราผ่าน แต่เครื่องเพื่อนพัง? วิธีนี้จะใช้ "คนกลาง" (Cloud) ตัดสิน ทำให้มาตรฐานเดียวกัน
2.  **แก้ปัญหา OS:** Windows ชอบมีปัญหาเรื่อง Path ยาวๆ หรือ Permission (EPERM Error ที่เราเจอ) แต่บน Linux Cloud ไม่มีปัญหานี้
3.  **ปลอดภัย:** เราไม่ต้องเอา Database Password ของจริงมาใส่ในเครื่องตัวเอง เราฝากไว้ที่ GitHub Secrets อย่างปลอดภัย

---

### 🔧 สรุปศัพท์เทคนิค (Technical Terms)
*   **CI (Continuous Integration):** การรวมโค้ดและทดสอบอัตโนมัติ (Build)
*   **CD (Continuous Deployment):** การเอาที่ Build ผ่านไปขึ้น Server จริงอัตโนมัติ (สเต็ปต่อไป)
*   **Workflow:** ไฟล์คำสั่งที่เราเขียนบอก GitHub ว่าต้องทำอะไรบ้าง
*   **Runner:** เครื่อง Server ของ GitHub ที่มารับงาน Build ให้เรา
