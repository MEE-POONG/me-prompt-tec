# 🔒 วิธีเปลี่ยน Repository เป็น Private

## ขั้นตอน:

1. **ไปที่ Settings:**
   ```
   https://github.com/MEE-POONG/me-prompt-tec/settings
   ```

2. **Scroll ลงไปด้านล่างสุด** ไปยังส่วน **"Danger Zone"**

3. **คลิก "Change repository visibility"**

4. **เลือก "Make private"**

5. **พิมพ์ชื่อ repository เพื่อยืนยัน:**
   ```
   MEE-POONG/me-prompt-tec
   ```

6. **คลิก "I understand, change repository visibility"**

---

## ✅ **ผลลัพธ์:**

หลังจากเปลี่ยนเป็น Private:
- ✅ มีแค่คุณและทีมที่เห็น code
- ✅ GitHub Actions ยังใช้งานได้ปกติ
- ✅ ปลอดภัยกว่า

---

## 🔐 **ตรวจสอบไฟล์ที่อาจมีข้อมูลลับ:**

ให้ตรวจสอบว่าไฟล์เหล่านี้ **ไม่ได้ถูก commit** ขึ้น GitHub:

- ❌ `.env`
- ❌ `.env.local`
- ❌ `.env.production`
- ❌ `config.json` (ถ้ามี secrets)

ตรวจสอบได้ที่:
```
https://github.com/MEE-POONG/me-prompt-tec/blob/master/.gitignore
```

---

## 📝 **สรุป:**

**ตอนนี้ปลอดภัยหรือไม่?**
- ✅ **ปลอดภัย** - เพราะยังไม่มี secrets ใน code
- ⚠️ **แนะนำ** - เปลี่ยนเป็น Private ดีกว่า
- ✅ **ไฟล์ `.env`** - ไม่ได้ถูก commit (มีใน .gitignore)

**ต้องการให้ผมช่วยอะไรเพิ่มเติมไหมครับ?**
