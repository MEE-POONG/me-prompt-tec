# 🚀 คู่มือ Build โปรเจกต์

## ปัญหาที่พบ
Build ไม่ผ่านบน Windows เพราะ EPERM error จาก symlink ใน `Application Data`

## ✅ วิธีแก้ (เลือก 1 วิธี)

---

## วิธีที่ 1: ใช้ WSL2 (แนะนำ - ง่ายที่สุด)

### ขั้นตอนที่ 1: เปิด WSL2
```bash
# เปิด PowerShell และพิมพ์
wsl
```

### ขั้นตอนที่ 2: ไปยัง Project Directory
```bash
cd /mnt/d/PJ/nuy/me-prompt-tec
```

### ขั้นตอนที่ 3: ติดตั้ง Dependencies (ถ้ายังไม่มี)
```bash
npm install
npx prisma generate
```

### ขั้นตอนที่ 4: Build
```bash
npm run build
```

### ขั้นตอนที่ 5: ตรวจสอบผลลัพธ์
```bash
ls -la .next
```

✅ **เสร็จแล้ว!** ไฟล์ build จะอยู่ใน `.next` folder

---

## วิธีที่ 2: Build ใน Docker (สำหรับ Production)

### ขั้นตอนที่ 1: Build Docker Image
```bash
docker build -t meprompt-web:latest .
```

### ขั้นตอนที่ 2: ตรวจสอบ Image
```bash
docker images | grep meprompt-web
```

### ขั้นตอนที่ 3: ทดสอบรัน Container
```bash
docker run -d \
  --name meprompt-test \
  -p 3000:3000 \
  --env-file .env \
  meprompt-web:latest
```

### ขั้นตอนที่ 4: ดู Logs
```bash
docker logs -f meprompt-test
```

### ขั้นตอนที่ 5: ทดสอบ
เปิดเบราว์เซอร์ไปที่ `http://localhost:3000`

---

## วิธีที่ 3: แก้ปัญหา Windows Symlink (ยาก - ไม่แนะนำ)

### ตรวจสอบ Symlinks
```powershell
Get-ChildItem -Path "C:\Users\PH" -Force | Where-Object { $_.LinkType -ne $null }
```

### ลบ Symlink ที่เป็นปัญหา (⚠️ ระวัง!)
```powershell
# ไม่แนะนำ - อาจทำให้ระบบเสียหาย
```

---

## 🎯 สรุป: ใช้วิธีไหนดี?

| วิธี | ความยาก | เวลา | แนะนำ |
|------|---------|------|-------|
| **WSL2** | ⭐ ง่าย | 2-3 นาที | ✅ **แนะนำ** |
| **Docker** | ⭐⭐ ปานกลาง | 5-10 นาที | ✅ สำหรับ Production |
| **แก้ Windows** | ⭐⭐⭐⭐⭐ ยากมาก | ไม่แน่นอน | ❌ ไม่แนะนำ |

---

## 📌 คำแนะนำ

1. **สำหรับทดสอบ:** ใช้ WSL2
2. **สำหรับขึ้น Production:** ใช้ Docker
3. **ถ้าไม่มี WSL2:** ติดตั้งก่อน
   ```powershell
   wsl --install
   ```

---

## ❓ คำถามที่พบบ่อย

### Q: ถ้าไม่มี WSL2 ต้องทำยังไง?
A: ติดตั้งด้วยคำสั่ง `wsl --install` ใน PowerShell (Admin)

### Q: Build ใน WSL2 แล้วใช้บน Windows ได้ไหม?
A: ได้! ไฟล์ `.next` จะอยู่ใน project folder เหมือนเดิม

### Q: Docker build ช้ามาก ทำยังไง?
A: ใช้ WSL2 แทน หรือรอให้ build เสร็จ (ครั้งแรกจะช้า ครั้งต่อไปเร็วขึ้น)

---

## 🔧 Troubleshooting

### ปัญหา: WSL2 ไม่มี Node.js
```bash
# ติดตั้ง Node.js ใน WSL2
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt-get install -y nodejs
```

### ปัญหา: Permission denied ใน WSL2
```bash
# แก้ไข permissions
sudo chown -R $USER:$USER /mnt/d/PJ/nuy/me-prompt-tec
```

### ปัญหา: Docker build ล้มเหลว
```bash
# ลบ cache และ build ใหม่
docker system prune -a
docker build --no-cache -t meprompt-web:latest .
```
